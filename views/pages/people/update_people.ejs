<%- include('../partials/header_admin'); %>
<main>
  <h2 class="titre">Modifier un utilisateur</h2>
  <div class="row align-center">
<% for (const person of model) { %> 
    <form action="/people/update_people/<%= person.pe_id %>" method="post">
      <div>
        <label for="LastName">Nom</label>
        <input required name="LastName" value ="<%= person.pe_lname%>"/>
      </div>

      <div>
        <label for="FirstName">Prénom</label>
        <input required name="FirstName" value ="<%= person.pe_fname%>"/>
      </div>
      
      
      <div>
          <label for="Email">Adresse e-mail</label>
          <input required name="Email" value ="<%= person.pe_email%>"/>
      </div>

      <div>
        <input required type='hidden' name="password" value="<%= person.pe_password%>"/>
    </div>

      <% } %> 
      <div>
        <button class="btn indigo"><a class="white-text" href="/people">Annuler</a></button>
        <input class="btn indigo" type="submit" value="Modifier"/>
      </div>
    </form>
  </div>
</main>


<%- include("footer") -%>